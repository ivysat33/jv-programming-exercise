# JV Programming Exercise

## Overview
This will provide a basic fetching service for information about a given github user's activity and repos

## Installation
To get started with this project, clone the repository and install the required gems. You can do this by running:

```
bundle install
```